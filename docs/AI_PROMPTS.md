# AI-assisted development prompt sequence

1. "Read the assignment brief and convert the requirements into a component checklist for a desktop Airbnb listing clone."
2. "Create a React + Vite project with reusable components for header, listing gallery, booking card, photo tour and lightbox."
3. "Implement keyboard-accessible lightbox navigation using Escape, ArrowLeft and ArrowRight."
4. "Create responsive CSS but prioritize desktop fidelity as required by the brief."
5. "Move listing content and image URLs into a JSON data file."
6. "Review the implementation for accessibility: semantic buttons, labels, focusable controls and dialog semantics."
7. "Create a production-scale vacation-rental marketplace architecture covering frontend, backend, storage, search and deployment."
8. "Run a final UI/interaction checklist against the assignment requirements and fix incomplete interactions."
